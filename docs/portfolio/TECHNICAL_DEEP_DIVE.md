# Technical Deep Dive

The evaluator bound source hashes, recipes, runtime closures, masks, schemas, and predecessor reports. Candidate execution was isolated from the evaluator. Hard masks were independently reconstructed. Measurements aggregated frames into episodes before folds. The Oracle used frozen polylines and direction labels, then sealed A/B truth until the human review was frozen.
