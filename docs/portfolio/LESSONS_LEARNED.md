# Lessons Learned

Local mathematical correctness is not distributional usefulness. Measurement must be independent from rendering. Synthetic success needs a real activation check. A numerical delta needs native visual validation. Fail-closed gates protect conclusions from motivated parameter drift.
