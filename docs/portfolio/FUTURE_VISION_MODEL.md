# Future Vision Model

A future teacher model could predict contour identity, side direction, semantic protection, and confidence from owned training data. A deterministic student would consume those maps. This remains a proposal; the Oracle result says that a new renderer objective is also required, not merely better labels.
