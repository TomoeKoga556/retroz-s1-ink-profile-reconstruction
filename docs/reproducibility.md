# Reproducibility

Run `make setup`, `make test`, `make synthetic-demo`, `make report`, `make docs-build`, and `make verify`. Public commands need no private data. Exact real-frame replay is described only in the physically separate `DO_NOT_UPLOAD` bundle.
