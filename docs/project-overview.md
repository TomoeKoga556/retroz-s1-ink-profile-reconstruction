# Project Overview

S1 isolates line-profile reconstruction from color, grain, halation, scan tonality, and temporal behavior. The objective was to reshape the two halves of an ink edge while holding the perceived midpoint and protected regions fixed. Five lineages and a human-verified Oracle were evaluated.
