# Architecture

The public package separates image I/O, public demonstration masks, candidate runtimes, contour geometry, synthetic fixtures, CLI orchestration, and provenance sidecars. The historical common evaluator is documented rather than copied wholesale because its real-data closure is private and platform-specific.
