# Reports in the Documentation Site

The canonical long-form Markdown and PDF live in the repository-level `reports/` directory. Run `make report` to regenerate both from the public-safe build script.
