# Security

Do not report secrets in public issues. The package performs no network access, model API calls, or automatic media downloads. Treat arbitrary label JSON and image files as untrusted input and run them in a constrained environment.
